{% extends 'base.html.twig' %}

{% block title %}Controls{% endblock %}

{% block body %}
    <h1>Controls</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ control.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ control.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_controls_index') }}">back to list</a>

    <a href="{{ path('app_controls_edit', {'id': control.id}) }}">edit</a>

    {{ include('controls/_delete_form.html.twig') }}
{% endblock %}
